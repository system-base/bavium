import { defineApp } from "convex/server";

// Convex app configuration.
// Required for Convex component integration (e.g. @convex-dev/workflow).
// Currently empty — add component registrations here as needed.
const app = defineApp();

export default app;
